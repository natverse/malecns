% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ids.R
\name{mcns_ids}
\alias{mcns_ids}
\title{Get Male CNS ids in standard formats}
\usage{
mcns_ids(
  ids,
  mustWork = TRUE,
  as_character = TRUE,
  integer64 = FALSE,
  unique = FALSE,
  ...,
  dataset = getOption("malecns.dataset")
)
}
\arguments{
\item{ids}{Either numeric ids (in \code{character}, \code{numeric},
\code{integer} or \code{integer64}format) or a query expression}

\item{mustWork}{Whether to insist that at least one valid id is returned
(default \code{TRUE})}

\item{as_character}{Whether to return segments as character rather than
numeric vector (the default is character for safety).}

\item{integer64}{whether to return ids with class bit64::integer64.}

\item{unique}{Whether to ensure that only unique ids are returned (default
\code{TRUE})}

\item{...}{Additional arguments passed to \code{\link[neuprintr]{neuprint_search}}}

\item{dataset}{The name of the dataset as reported in Clio e.g. CNS, VNC etc}
}
\value{
A vector of numeric ids with mode determined by \code{as_character}
  and \code{integer64}
}
\description{
Get Male CNS ids in standard formats
}
\examples{
mcns_ids("DA2_lPN")
mcns_ids("DA2_lPN", integer64=TRUE)
mcns_ids("/VL2a.+")

\dontrun{
# throws an error
mcns_ids("rhubarb")
}
# returns a length 0 vector
mcns_ids("rhubarb", mustWork = FALSE)
}
